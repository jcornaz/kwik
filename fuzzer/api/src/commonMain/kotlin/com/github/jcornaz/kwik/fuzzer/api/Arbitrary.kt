package com.github.jcornaz.kwik.fuzzer.api

/**
 * This is an empty object for declaring [SimpleFuzzer] factories as extension function on [Arbitrary].
 *
 * So that one can use [Arbitrary] to easily find any [SimpleFuzzer] factory, regardless on the module it comes from.
 */
object Arbitrary
